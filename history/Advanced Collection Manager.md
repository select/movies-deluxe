in the collection admin allow to store search terms, filter paramters for gener, year, num votes, ... (combinations as a list) as part of a collection, allow to refesh the collection by having buttons for each stored search terms and filter combinations below Search Library search field, allow to add and remove filter/seach combinations, sync to collections.json via API, next to title desc add tags to collection and allow to edit
