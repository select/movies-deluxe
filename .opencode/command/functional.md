---
description: make the code more functional
---

Replace `for` loops with map, filter, reduce, ...
Replace `if` if early return with `if (..) return;`
Replace `if` with ternary if convenient.

$ARGUMENTS
